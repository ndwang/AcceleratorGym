Scan `{corrector_var}` through {n} evenly spaced values from 0 to {max_kick} rad. At each step, read the horizontal orbit at `{bpm_var}`. Restore the corrector when done.

Return `{{"data": [[<kick>, <orbit>], ...]}}`.
