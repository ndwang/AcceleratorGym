Set the following horizontal correctors to zero: {corrector_variables}. Then read the horizontal orbit at these BPMs: {bpm_variables}.

Return `{{"orbit": {{"<bpm_name>": <orbit_in_meters>, ...}}}}`.
