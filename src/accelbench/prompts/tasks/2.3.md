Apply a kick of {kick} rad to `{corrector_var}`, read the horizontal orbit at the following BPMs: {bpm_variables}, then restore the corrector to its original value. Report the orbit change at each BPM.

Return `{{"orbit_change": {{"<bpm_name>": <change_in_meters>, ...}}}}`.
